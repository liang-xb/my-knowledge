---
aside: right
outline: [2, 3]
---

<h1 style="text-align: center; font-weight: bold;">application.yml</h1>

---

## 模板文件

```yml
#服务配置
server:
  #端口
  port: 9995
  #服务编码
  tomcat:
    uri-encoding: UTF-8

spring:
  application:
    name: zzyl # 替换为自己的项目名称
  jackson:
    date-format: yyyy-MM-dd HH:mm:ss
  mvc: # 解决 Springboot 版本导致 swagger 不兼容问题
    pathmatch:
      matching-strategy: ant_path_matcher
  main:
    # 允许 Bean 同名覆盖，后注册的 Bean 会把先注册的 Bean 覆盖
    allow-bean-definition-overriding: true
    # 允许循环依赖
    allow-circular-references: true
  redis:
    host: 127.0.0.1
    port: 6379
    password:
  #数据源配置
  datasource:
    # 记得加 druid 的依赖
    druid:
      driver-class-name: com.mysql.jdbc.Driver
      # 替换成自己的数据库名
      url: jdbc:mysql://localhost:3306/zzyl?serverTimezone=Asia/Shanghai&useUnicode=true&characterEncoding=utf-8&zeroDateTimeBehavior=convertToNull&useSSL=false&allowPublicKeyRetrieval=true
      # 替换为自己的账号密码
      username: root
      password: 123456

# MyBatis配置
mybatis:
  # mapper配置文件
  mapper-locations: classpath*:mapper/*.xml
  type-aliases-package: com.zzyl.entity # 替换成自己的
  configuration:
    # 这个配置会将执行的 sql 打印出来，在开发或测试的时候可以用
    log-impl: org.apache.ibatis.logging.stdout.StdOutImpl
    # 驼峰下划线转换
    map-underscore-to-camel-case: true
    # 开启主键返回
    use-generated-keys: true
    default-statement-timeout: 60
    default-fetch-size: 100

# 配置日志输出
logging:
  level:
    # WebSocket 连接 / 消息问题
    org.springframework.web.socket: debug
    # 事务输出
    # org.springframework.jdbc.support.JdbcTransactionManager: debug
  # 配置 logback.xml
  config: classpath:logback.xml

# 美化与增强 swagger 文档
knife4j:
  enable: true

# 项目相关配置
zzyl:
 swagger:
    swagger-path: com.zzyl.controller
    title:
    description:
    contact-name:
    contact-url:
    contact-email:
 jwt:
    base64-encoded-secret-key: $2a$10$PVtHnkj86mJgf6li/yron.LRx/cQAlaiZkBJ9BeogCNTryXJRT1YC
    ttl: 3600000
 oss:
    endpoint:
    bucketName:
    accessKeyId:
    accessKeySecret:
  wechat:
    appId:
    appSecret:
```

## logback.xml 模板

```xml
<?xml version="1.0" encoding="UTF-8"?>
<configuration debug="false">
	<!--定义日志文件的存储地址 勿在 LogBack 的配置中使用相对路径 -->
	<property name="LOG_HOME" value="../logs" />
	<!-- 控制台输出 -->
	<appender name="STDOUT" class="ch.qos.logback.core.ConsoleAppender">
		<encoder class="ch.qos.logback.classic.encoder.PatternLayoutEncoder">
			<!--格式化输出：%d表示日期，%thread表示线程名，%-5level：级别从左显示5个字符宽度%msg：日志消息，%n是换行符 -->
			<pattern>%d{yyyy-MM-dd HH:mm:ss.SSS} [%thread] %-5level %logger{50}-%msg%n</pattern>
		</encoder>
	</appender>

	<!-- 按照每天生成日志文件 -->
	<appender name="FILE"
		class="ch.qos.logback.core.rolling.RollingFileAppender">
		<rollingPolicy class="ch.qos.logback.core.rolling.TimeBasedRollingPolicy">
			<!--日志文件输出的文件名 -->
			<FileNamePattern>${LOG_HOME}/zzyl.log.%d{yyyy-MM-dd}.log
			</FileNamePattern>
			<!--日志文件保留天数 -->
			<MaxHistory>30</MaxHistory>
		</rollingPolicy>
		<encoder class="ch.qos.logback.classic.encoder.PatternLayoutEncoder">
			<!--格式化输出：%d表示日期，%thread表示线程名，%-5level：级别从左显示5个字符宽度%msg：日志消息，%n是换行符 -->
			<pattern>%d{yyyy-MM-dd HH:mm:ss.SSS} [%thread] %-5level %logger{50}-%msg%n</pattern>
		</encoder>
		<!--日志文件最大的大小 -->
		<triggeringPolicy
			class="ch.qos.logback.core.rolling.SizeBasedTriggeringPolicy">
			<MaxFileSize>10MB</MaxFileSize>
		</triggeringPolicy>
	</appender>
	<!-- show parameters for hibernate sql 专为 Hibernate 定制 -->
	<logger name="org.hibernate.type.descriptor.sql.BasicBinder"
		level="TRACE" />
	<logger name="org.hibernate.type.descriptor.sql.BasicExtractor"
		level="DEBUG" />
	<logger name="org.hibernate.SQL" level="DEBUG" />
	<logger name="org.hibernate.engine.QueryParameters" level="DEBUG" />
	<logger name="org.hibernate.engine.query.HQLQueryPlan" level="DEBUG" />

	<!--myibatis log configure -->
	<logger name="com.apache.ibatis" level="TRACE" />
	<logger name="java.sql.Connection" level="DEBUG" />
	<logger name="java.sql.Statement" level="DEBUG" />
	<logger name="java.sql.PreparedStatement" level="DEBUG" />

	<!-- 日志输出级别 -->
	<root level="info">
		<appender-ref ref="STDOUT" />
		<appender-ref ref="FILE" />
	</root>
	<!--日志异步到数据库 -->
</configuration>
```
