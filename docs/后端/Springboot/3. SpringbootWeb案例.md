---
outline: [2, 3]
aside: right
---

# SpringBoot Web 案例

---

## 需求实现

访问前端静态页面后，前端发送 Ajax 请求获取 JSON 数据并展示。

## 实现步骤

### 实体类

```java
@Data
@NoArgsConstructor
@AllArgsConstructor
public class User {
    private Integer id;
    private String username;
    private String password;
    private String name;
    private Integer age;
    private LocalDateTime updateTime;
}
```

| 注解 | 说明 |
| ---- | ---- |
| `@Data` | Lombok 注解，提供 getter/setter 方法 |
| `@NoArgsConstructor` | 无参构造器 |
| `@AllArgsConstructor` | 全参构造器 |

### Controller

```java
@RestController
public class UserController {
    @RequestMapping("/list")
    public List<User> list() throws Exception {
        // 1. 加载并读取文件
        InputStream inputStream = this.getClass().getClassLoader()
                .getResourceAsStream("user.txt");
        ArrayList<String> lines = IoUtil.readLines(inputStream, StandardCharsets.UTF_8, new ArrayList<>());

        // 2. 解析数据，封装成对象集合
        List<User> userList = lines.stream().map(line -> {
            String[] parts = line.split(",");
            Integer id = Integer.parseInt(parts[0]);
            String username = parts[1];
            String password = parts[2];
            String name = parts[3];
            Integer age = Integer.parseInt(parts[4]);
            LocalDateTime updateTime = LocalDateTime.parse(parts[5],
                    DateTimeFormatter.ofPattern("yyyy-MM-dd HH:mm:ss"));
            return new User(id, username, password, name, age, updateTime);
        }).collect(Collectors.toList());

        // 3. 响应数据
        return userList;
    }
}
```

> 推荐使用 Hutool 工具库简化文件读取等操作：

```xml
<dependency>
    <groupId>cn.hutool</groupId>
    <artifactId>hutool-all</artifactId>
    <version>5.8.27</version>
</dependency>
```

## @ResponseBody

- **类型**：方法注解、类注解
- **位置**：Controller 方法上或类上
- **作用**：将方法返回值直接响应给浏览器，若返回值是对象/集合，则转换为 JSON 格式

> `@RestController` 包含了 `@Controller` + `@ResponseBody`，因此类上加 `@RestController` 后所有方法都自动具有 `@ResponseBody` 的效果。
