---
outline: [2, 3]
aside: right
---

# InnoDB 引擎

---

## 逻辑存储结构

![逻辑存储结构](逻辑存储结构.png)

### 表空间

表空间是 InnoDB 存储引擎逻辑结构的最高层。如果启用了参数 **innodb_file_per_table**（8.0 版本中默认开启），则每张表都会有一个 **表空间**（**xxx.ibd**），一个 MySQL 实例可以对应多个表空间，用于存储记录、索引等数据。

### 段

段分为 **数据段**（Leaf node segment）、**索引段**（Non-leaf node segment）、**回滚段**（Rollback segment）。InnoDB 是索引组织表，**数据段** 就是 B+ 树的 **叶子节点**，**索引段** 即为 B+ 树的 **非叶子节点**，段用来管理多个 Extent（区）。

### 区

区是表空间的单元结构，每个区的大小为 **1MB**。默认情况下，InnoDB 存储引擎 **页大小为 16KB**，即一个区中一共有 **64** 个连续的页。

### 页

页是 InnoDB 存储引擎磁盘管理的最小单元，每个页的大小默认为 **16KB**。为了保证页的连续性，InnoDB 存储引擎每次从磁盘申请 **4-5** 个区。

### 行

InnoDB 存储引擎是面向行的，数据是按行进行存放的。

## 内存结构

![内存结构](内存结构.png)

### Buffer Pool

![Buffer Pool](buffer_pool.png)

Buffer Pool 是 InnoDB 存储引擎的内存缓冲区，用于缓存磁盘上的数据页和索引页。当查询数据时，首先检查 Buffer Pool 中是否有缓存，如果没有再从磁盘读取并加载到 Buffer Pool 中。

Buffer Pool 以 **页** 为单位，默认每页 16KB。通过参数 `innodb_buffer_pool_size` 设置大小。

### Change Buffer

![Change Buffer](change_buffer.png)

Change Buffer 是 Buffer Pool 的一部分，用于缓存对二级索引页的修改操作。当二级索引页不在 Buffer Pool 中时，InnoDB 会将对二级索引的修改缓存在 Change Buffer 中，等未来相关数据被读取时，再将 Change Buffer 中的操作合并到对应的二级索引页中。

### Log Buffer

![Log Buffer](log_buffer.png)

Log Buffer 用于缓存 redo log 日志，默认大小为 16MB。当事务提交时，Log Buffer 中的日志会写入到磁盘上的 redo log 文件中。

## 磁盘结构

![磁盘结构](磁盘结构-1.png)

![磁盘结构2](磁盘结构-2.png)

### 系统表空间

![系统表空间1](系统表空间-1.png)

![系统表空间2](系统表空间-2.png)

### 逻辑结构

![逻辑结构](逻辑结构.png)

## 事务原理

### 事务特性实现

ACID 四大特性在 InnoDB 中的实现：

| 特性 | 实现机制 |
| ---- | -------- |
| 原子性（Atomicity） | Undo Log（回滚日志） |
| 一致性（Consistency） | Redo Log + Undo Log |
| 隔离性（Isolation） | MVCC + 锁 |
| 持久性（Durability） | Redo Log（重做日志） |

### Redo Log

Redo Log 记录了数据页的物理修改，用于保证事务的持久性。当事务提交时，先将 Redo Log 刷盘，再修改 Buffer Pool 中的数据（Write-Ahead Logging 策略）。

![Redo Log1](redo_log-1.png)

![Redo Log2](redo_log-2.png)

### Undo Log

Undo Log 记录了数据修改前的逻辑状态，用于回滚事务和 MVCC。

## MVCC（多版本并发控制）

### 基本概念

MVCC（Multi-Version Concurrency Control）是通过维护数据的多个版本来实现数据库的并发控制。在 InnoDB 中，MVCC 通过 **Undo Log + ReadView** 实现，解决了读写冲突，实现了非锁定读。

### 隐藏字段

InnoDB 为每行记录添加了三个隐藏字段：

![隐藏字段1](隐藏字段-1.png)

![隐藏字段2](隐藏字段-2.png)

| 字段 | 说明 |
| ---- | ---- |
| DB_TRX_ID | 最近修改该行的事务 ID |
| DB_ROLL_PTR | 回滚指针，指向该行的 Undo Log |
| DB_ROW_ID | 行 ID（如果没有主键和唯一索引，InnoDB 自动生成） |

### ReadView

ReadView 是快照读 SQL 执行时 MVCC 提取数据的依据，记录并维护当前活跃的事务 ID。

ReadView 判断版本链中哪个版本数据可见的规则：

![ReadView1](readview-1.png)

![ReadView2](readview-2.png)

### 隔离级别与 MVCC

| 隔离级别 | ReadView 生成时机 |
| -------- | ----------------- |
| READ COMMITTED | 每次 SELECT 都生成新的 ReadView |
| REPEATABLE READ | 事务第一次 SELECT 时生成 ReadView，之后复用 |
